<UserControl x:Class="WebForms2BlazorWASM.Dialog.SetupBlazorDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="440" Height="260">

    <UserControl.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,12"/>
            <Setter Property="Padding" Value="4,3"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="0,0,0,2"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Width" Value="90"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Setup Blazor WASM Projects"
                   FontSize="15" FontWeight="SemiBold" Margin="0,0,0,16"/>

        <!-- Client Project Name -->
        <Label Grid.Row="1" Content="Client Project Name"/>
        <TextBox Grid.Row="2" x:Name="txtClientName" Text="WingtipToys.Client"/>

        <!-- Server Project Name -->
        <Label Grid.Row="3" Content="Server Project Name"/>
        <TextBox Grid.Row="4" x:Name="txtServerName" Text="WingtipToys.Server"/>

        <!-- Shared Project Name -->
        <Label Grid.Row="5" Content="Shared Project Name"/>
        <TextBox Grid.Row="6" x:Name="txtSharedName" Text="WingtipToys.Shared"
                 VerticalAlignment="Top"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Click="Cancel_Click"/>
            <Button Content="Run" Click="Run_Click" IsDefault="True"
                    Background="#0078D4" Foreground="White"/>
        </StackPanel>
    </Grid>

</UserControl>
